<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Title="{Binding Name}"
             x:Class="IdentifyMe.App.Views.Connections.ConnectionsPage">
    <ContentPage.Content>
        <StackLayout VerticalOptions="Start">
            <Label Text="Welcome to Connections Page!"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />
            <Entry Text="{Binding InvitationMessageUrl, Mode=TwoWay}" />
            <Button Text="Scand" Command="{Binding OpenScannerPageCommand}" />
            <Button Text="Connect" Command="{Binding AcceptConnectionCommand}" />
            <Label Text="Result:" />
            <Label Text="{Binding QRScanedResult, Mode=TwoWay}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>